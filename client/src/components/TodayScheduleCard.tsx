/**
 * TodayScheduleCard — Removed. Replaced by UpcomingDatesCard in the grid.
 * This file now exports a placeholder that returns null.
 * The UpcomingDatesCard handles the "upcoming dates" display.
 */
export default function TodayScheduleCard({ delay = 0 }: { delay?: number }) {
  return null;
}
